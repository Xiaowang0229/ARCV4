<ui:FluentWindow
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:ARCV4.Views"
        x:Class="ARCV4.Views.UpdateWindow"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="自动点名器 更新窗口" Height="602" Width="550" Loaded="FluentWindow_Loaded" Background="{x:Null}">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" 
        Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Style>
        </Grid.Resources>
        <ui:TitleBar Title="检测更新" CanMaximize="False" ShowMaximize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon x:Name="RootIcon"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <ui:Button x:Name="ExitButton" Content="取消" Height="45" Margin="389,532,0,0" VerticalAlignment="Top" Width="151" IsEnabled="False" Click="ExitButton_Click"/>
        <ui:CardExpander x:Name="RootExpander"  HorizontalAlignment="Center" Height="457"  Margin="0,53,0,0" VerticalAlignment="Top" Width="530" IsEnabled="False">
            <ui:CardExpander.Header>
                <Grid Height="60">
                    <ui:Image x:Name="RootImage2" HorizontalAlignment="Left" Margin="14,0,0,0" VerticalAlignment="Center" Height="48" Width="48"/>
                    <ui:TextBlock x:Name="ProductName" HorizontalAlignment="Left" Margin="79,3,0,0" TextWrapping="Wrap" Text="自动点名器" VerticalAlignment="Top" Height="30" Width="277" FontWeight="Bold" FontSize="24"/>
                    <ui:TextBlock x:Name="DescribeBlock" HorizontalAlignment="Left" Margin="79,37,0,0" TextWrapping="Wrap" Text="正在检测更新……" VerticalAlignment="Top" Width="277"/>

                </Grid>
            </ui:CardExpander.Header>
            <Grid Height="325">
                <ui:Button Content="立即更新" Margin="360,280,0,0" VerticalAlignment="Top" Width="130" Click="Button_Click" Height="35"/>
                <ui:RichTextBox x:Name="RootBox" IsReadOnly="True" Margin="0,0,0,50">
                    <FlowDocument>
                        <Paragraph>
                            <Run x:Name="RootRun" FontFamily="Microsoft YaHei UI"/>
                        </Paragraph>
                    </FlowDocument>
                </ui:RichTextBox>
            </Grid>
        </ui:CardExpander>

    </Grid>
</ui:FluentWindow>
